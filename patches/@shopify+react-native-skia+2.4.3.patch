diff --git a/node_modules/@shopify/react-native-skia/android/build.gradle b/node_modules/@shopify/react-native-skia/android/build.gradle
index 9c6ab08..3bf3157 100644
--- a/node_modules/@shopify/react-native-skia/android/build.gradle
+++ b/node_modules/@shopify/react-native-skia/android/build.gradle
@@ -178,7 +178,14 @@ android {
             srcDirs += ['../libs/android']
         }
         java {
+            // When the New Architecture is enabled, include Fabric (paper) sources.
+            // When it's disabled (the default for your app), explicitly exclude them
+            // so only the classic/old-arch Skia sources are compiled.
             if (!isNewArchitectureEnabled()) {
+                // Do NOT compile Fabric / paper sources when new arch is off
+                exclude 'src/paper/**'
+            } else {
+                // New Architecture: include Fabric sources
                 srcDirs += [
                     "src/paper/java",
                 ]
